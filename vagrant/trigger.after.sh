#!/usr/bin/env bash
#
# Restarts the apache2 and other services
# (Shared folders are made available *after* the services start, so any shared folders that are used as document-root
# directives will)
#

#echo "nothing to do here"
#vagrant ssh -c "sudo service apache2 restart; ps -ef | grep apache2"
